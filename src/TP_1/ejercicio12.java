/*Escribe un programa que lea como dato de entrada una fecha expresada en días (1-31), mes (1-12) y año (en número) y nos dice la fecha que será al día siguiente. Considere, los años bisiestos */
package TP_1;
import java.util.Scanner;

public class ejercicio12 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int dias, mes, año;

        do {
            System.out.println("Ingrese la fecha en dias (1-31): ");
            dias = sc.nextInt();
            System.out.println("Ingrese la fecha en meses (1-12): ");
            mes = sc.nextInt();
            System.out.println("Ingrese la fecha en años (numero): ");
            año = sc.nextInt();

            if 
        }
    }    
}
